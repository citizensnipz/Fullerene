# Fullerene — Claude Code (harness)

Claude Code loads this from `.claude/rules/`. It does **not** replace root `CLAUDE.md`; both should stay short.

**Start at:** `ai/MEMORY.md` (index, workflows, harness-vs-repo rule, working agreements).

Optional later: `.claude/settings.json`, `.claude/skills/` — see Claude Code docs.
